        .org 000h       ; Начальный адрес (для CP/M или простого вывода)

start:  mvi c, 02h      ; Функция вывода символа (может зависеть от системы)
        mvi e, 40h      ; Символ 'A'
        Call  5         ;  Вызов системной функции (для теста синтаксиса)

        jmp start       ; Бесконечный цикл


        .end start
   